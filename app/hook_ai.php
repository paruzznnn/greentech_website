<?php
if (isset($_GET)) {
    if ($_GET['action'] === 'getreply') {
        // $reply_message = isset($_GET['reply']) ? $mysqli->escape_string($_GET['reply']) : "";
        // $code = "UPDATE live_chat SET reply_message = '{$reply_message}' WHERE id = '{$id}'";
        // $mysqli->query($code);
    }
}